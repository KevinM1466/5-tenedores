/** @format */

import { StyleSheet } from "react-native";
import { color } from "../../../utils";

export const styles = StyleSheet.create({
	content: {
		flex: 1,
	},
	icon: {
		position: "absolute",
		bottom: 10,
		right: 10,
	},
});
